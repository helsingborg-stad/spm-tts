import XCTest
    @testable import TTS

    final class TTSTests: XCTestCase {
        func testExample() {
            // This is an example of a functional test case.
            // Use XCTAssert and related functions to verify your tests produce the correct
            // results.
            XCTAssertEqual(TTS().text, "Hello, World!")
        }
    }
